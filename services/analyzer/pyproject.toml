[project]
name = "vaexcore-pulse-analyzer"
version = "0.1.0"
description = "Offline analyzer scaffold for vaexcore pulse"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
