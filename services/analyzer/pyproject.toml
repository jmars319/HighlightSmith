[project]
name = "highlightsmith-analyzer"
version = "0.1.0"
description = "Offline analyzer scaffold for HighlightSmith"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
