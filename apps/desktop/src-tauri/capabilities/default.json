{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the HighlightSmith desktop shell.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
